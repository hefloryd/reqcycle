package org.polarsys.reqcycle.repository.connector.document;

public interface IDOCConstants {
	public static final String DOC_NAME = Activator.PLUGIN_ID + ".doc.name";
	//selected requirements in order to generate
	public static final String LIST_VALIDE_REQ = Activator.PLUGIN_ID + ".list.valide.requirement";
	
}
